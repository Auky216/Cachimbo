/**
 * Propuesta de Card que sea lo mas adptable para algunas de las secciones
 */

const CardEvent = () => {
  return (
    <article>{/* depende de la seccion, abarca la info compacta */}</article>
  );
};

export default CardEvent;
